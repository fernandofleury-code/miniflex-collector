{
  "name": "miniflex-collector",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema completo para catalogos, colecionadores e rankings MiniFlex.",
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "dotenv": "^17.4.2",
    "express": "^5.1.0"
  }
}
